---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/sections/Navbar.astro';
import Footer from '../components/sections/Footer.astro';
import { AlertTriangle, Home } from 'lucide-react';
---

<BaseLayout title="Error 404 | Sistema No Encontrado">
  <Navbar />
  
  <main class="min-h-screen flex items-center justify-center relative overflow-hidden">
    <div class="absolute inset-0 bg-space-950">
        <div class="absolute inset-0 bg-[url('/grid.svg')] opacity-10"></div>
    </div>

    <div class="relative z-10 text-center px-6 max-w-2xl">
      <div class="inline-flex items-center justify-center w-24 h-24 rounded-full bg-red-500/10 text-red-500 mb-8 border border-red-500/20 shadow-[0_0_30px_rgba(239,68,68,0.2)]">
        <AlertTriangle size={48} />
      </div>

      <h1 class="text-6xl md:text-8xl font-bold text-text-main mb-4 font-display tracking-tighter">
        404
      </h1>
      
      <h2 class="text-2xl font-bold text-white mb-6">
        Conexión Interrumpida
      </h2>
      
      <p class="text-text-muted text-lg mb-10">
        La ruta que intentas explorar no existe en nuestra infraestructura. <br />
        Es posible que el enlace esté roto o la página haya sido migrada.
      </p>

      <a href="/" class="inline-flex items-center gap-2 px-8 py-4 bg-tech-blue text-white font-bold rounded-lg hover:bg-neon-cyan hover:text-space-950 transition-all shadow-lg hover:shadow-neon-cyan/25">
        <Home size={20} />
        Regresar al Inicio
      </a>
      
      <div class="mt-12 text-xs font-mono text-space-800">
        ERROR_CODE: PAGE_NOT_FOUND_EXCEPTION
      </div>
    </div>
  </main>

  <Footer />
</BaseLayout>
