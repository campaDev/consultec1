---
import { SITE_INFO } from '../../data/site-info';
import { MapPin, Phone, Mail, Facebook, Instagram } from 'lucide-react';

const currentYear = new Date().getFullYear();
---

<footer class="bg-space-950 pt-24 pb-12 border-t border-space-800">
  <div class="max-w-7xl mx-auto px-6 grid md:grid-cols-4 gap-12 mb-16">
    
    <div class="md:col-span-2 space-y-6">
      <a href="/" class="text-2xl font-bold text-text-main font-display">
        CONSUL<span class="text-neon-cyan">TEC</span>.
      </a>
      <p class="text-text-muted max-w-sm text-sm leading-relaxed">
        {SITE_INFO.description}
      </p>
      <div class="flex gap-4">
        <a href={SITE_INFO.social.facebook} class="p-2 bg-space-900 rounded-full text-text-main hover:bg-neon-cyan hover:text-black transition-colors">
            <Facebook size={18} />
        </a>
        <a href={SITE_INFO.social.instagram} class="p-2 bg-space-900 rounded-full text-text-main hover:bg-neon-cyan hover:text-black transition-colors">
            <Instagram size={18} />
        </a>
      </div>
    </div>

    <div>
      <h4 class="text-text-main font-bold mb-6">Contacto</h4>
      <ul class="space-y-4 text-sm text-text-muted">
        <li class="flex items-start gap-3">
            <MapPin size={18} class="text-neon-cyan shrink-0 mt-0.5" />
            <span>{SITE_INFO.address.street}<br/>{SITE_INFO.address.city}, {SITE_INFO.address.region}</span>
        </li>
        <li class="flex items-center gap-3">
            <Phone size={18} class="text-neon-cyan shrink-0" />
            <a href={`tel:${SITE_INFO.phone}`} class="hover:text-text-main transition-colors">{SITE_INFO.phone}</a>
        </li>
        <li class="flex items-center gap-3">
            <Mail size={18} class="text-neon-cyan shrink-0" />
            <a href="mailto:info@consultec.com.py" class="hover:text-text-main transition-colors">info@consultec.com.py</a>
        </li>
      </ul>
    </div>

    <div>
      <h4 class="text-text-main font-bold mb-6">Links</h4>
      <ul class="space-y-3 text-sm text-text-muted">
        <li><a href="#servicios" class="hover:text-neon-cyan transition-colors">Servicios</a></li>
        <li><a href="#productos" class="hover:text-neon-cyan transition-colors">Productos</a></li>
        <li><a href="#" class="hover:text-neon-cyan transition-colors">Política de Privacidad</a></li>
        <li><a href="#" class="hover:text-neon-cyan transition-colors">Términos de Servicio</a></li>
      </ul>
    </div>
  </div>

  <div class="max-w-7xl mx-auto px-6 pt-8 border-t border-space-800 flex flex-col md:flex-row justify-between items-center text-xs text-text-muted gap-4">
    <p>&copy; {currentYear} {SITE_INFO.name}. Todos los derechos reservados.</p>
    <p>Desarrollado con AstroJS & Tailwind v4.</p>
  </div>
</footer>
